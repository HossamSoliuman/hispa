@extends('owner.layouts.master')
@section('title', __('owner.startup.menu.title'))
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">{{ __('owner.startup.menu.projects') }}</h1><p class="text-muted mb-0">{{ __('owner.startup.subtitle') }}</p></div><a href="{{ route('owner.startup.projects.create') }}" class="btn btn-theme"><i class="bi bi-plus-lg"></i> {{ __('owner.startup.add_project') }}</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="row g-3">@forelse($projects as $project)<div class="col-xl-4 col-md-6"><div class="card h-100 border-0 shadow-sm"><div class="card-body"><div class="d-flex justify-content-between"><span class="badge bg-theme bg-opacity-10 text-theme">{{ __('owner.startup.types.'.$project->type) }}</span><span class="badge bg-secondary">{{ __('owner.startup.statuses.'.$project->status) }}</span></div><h4 class="mt-3">{{ $project->name }}</h4><div class="text-muted small mb-3"><i class="bi bi-calendar3"></i> {{ $project->start_date->format('Y-m-d') }}</div><div class="d-flex gap-4 border-top pt-3"><div><strong>{{ $project->partners_count }}</strong><small class="d-block text-muted">{{ __('owner.startup.partners') }}</small></div><div><strong>{{ number_format($project->expenses_sum_amount ?? 0,2) }} <x-riyal-icon size="sm" /></strong><small class="d-block text-muted">{{ __('owner.startup.total_cost') }}</small></div></div></div><div class="card-footer bg-transparent d-flex align-items-center gap-2"><a class="btn btn-sm btn-theme flex-fill" href="{{ route('owner.startup.projects.show',$project) }}">{{ __('owner.startup.open') }}</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('owner.startup.projects.edit',$project) }}" title="{{ __('owner.startup.edit') }}"><i class="bi bi-pencil"></i></a><form method="POST" action="{{ route('owner.startup.projects.destroy',$project) }}" onsubmit="return confirm(@js(__('owner.startup.confirm_delete_project')))" class="m-0">@csrf @method('DELETE')<button type="submit" class="btn btn-sm btn-outline-danger" title="{{ __('owner.startup.delete') }}"><i class="bi bi-trash"></i></button></form></div></div></div>@empty<div class="col-12"><div class="card border-0 text-center py-5"><i class="bi bi-inbox fs-1 text-muted"></i><h5 class="mt-3">{{ __('owner.startup.empty') }}</h5></div></div>@endforelse</div><div class="mt-4">{{ $projects->links() }}</div>
@endsection
